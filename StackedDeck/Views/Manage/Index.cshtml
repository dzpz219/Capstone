@model StackedDeck.Models.IndexViewModel
@{
    ViewBag.Title = "User Management";
}

<div class="container margin-top-sm">
    <div>
        <h2>User Management</h2>
        <h3 class="text-success">@ViewBag.StatusMessage</h3>
        <hr />
        @Html.ActionLink("Change Password", "ChangePassword", "", new { @class = "btn btn-primary" })
        @Html.ActionLink("Update Information", "UpdateInformation", "", new { @class = "btn btn-primary" })
    </div>
</div>